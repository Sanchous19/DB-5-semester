use mydb;

SELECT city_type, street_type, house_number, flat, zip_code, phone_number, email FROM address;

SELECT name FROM area;

SELECT name FROM city;

SELECT name, short_name, phone_code FROM country;

SELECT name, short_name FROM faculty;

SELECT name FROM name1;

SELECT name FROM name2;

SELECT name FROM name3;

SELECT series, number, number_id, authority, date1, date2, date3 FROM passport;

SELECT name FROM region;

SELECT name FROM speciality;

SELECT name FROM street;

SELECT sex, course, group_number, card_number, date1, date2, operator_code, phone_number, date3 FROM student;

SELECT name, short_name FROM university;