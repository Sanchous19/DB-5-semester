use mydb;

SELECT city_type, street_type, house_number, flat, zip_code, phone_number, email FROM address LIMIT 5;

SELECT name FROM area LIMIT 5;

SELECT name FROM city LIMIT 5;

SELECT name, short_name, phone_code FROM country LIMIT 5;

SELECT name, short_name FROM faculty LIMIT 5;

SELECT name FROM name1 LIMIT 5;

SELECT name FROM name2 LIMIT 5;

SELECT name FROM name3 LIMIT 5;

SELECT series, number, number_id, authority, date1, date2, date3 FROM passport LIMIT 5;

SELECT name FROM region LIMIT 5;

SELECT name FROM speciality LIMIT 5;

SELECT name FROM street LIMIT 5;

SELECT sex, course, group_number, card_number, date1, date2, operator_code, phone_number, date3 FROM student LIMIT 5;

SELECT name, short_name FROM university LIMIT 5;